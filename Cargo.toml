[package]
name = "pacwoman"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "pacwoman"
path = "src/main.rs"

[dependencies]
alpm = "5"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
strip = true
